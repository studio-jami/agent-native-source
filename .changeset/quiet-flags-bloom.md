---
"@agent-native/core": minor
---

Add shared default-off feature flags with server-side targeting, versioned fleet-management actions, narrowly delegated A2A access, decision and explicit exposure APIs, and reusable controlled editor primitives.
